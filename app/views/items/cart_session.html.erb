<ul>
  <% if @cart.empty? %>
    <p>No items in cart.</p>
  <% else %>
    <% @cart.each do |item| %>
      <li><%= item.title %> - <%= link_to 'Remove from Cart', remove_from_cart_path(item.id), method: :delete %></li>
    <% end %>
  <% end %>
</ul>
