<%= form_with(model: item, local: true) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
        <% item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="label">
      <%= form.label :title %>
    </div>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <div class="label">
      <%= form.label :image %>
    </div>
    <%= form.file_field :image %>
  </div>

  <div class="field">
    <div class="label">
      <%= form.label :description %>
    </div>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <div class="label">
      <%= form.label :category %>
    </div>
    <%= form.select :category, ['Table', 'Desk', 'Chair', 'Couch', 'Lighting', 'Rug', 'Wall Hanging', 'Mirror', 'Other'], include_blank: true %>
  </div>

  <div class="field">
    <div class="label">
      <%= form.label :price %>
    </div>
    <%= form.number_field :price %>
  </div>

  <div class="actions back">
    <%= form.submit %>
  </div>
<% end %>
